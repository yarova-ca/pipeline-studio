# Platform build config - generated by Backstage scaffolder. Do not edit.

service:
  name: {{ values.name }}
  framework: {{ values.framework }}
  description: "{{ values.description }}"

compliance:
  standards: {{ values.compliance }}

deploy:
  target: {{ values.target }}
  registry: {{ values.registry }}

dockerBuild: "docker build --build-arg COMPLIANCE={{ values.compliance }} ."
