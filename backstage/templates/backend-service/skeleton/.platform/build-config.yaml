# Platform build config - generated by Backstage scaffolder. Do not edit.
# To regenerate: use Backstage Create with the same or updated parameters.

service:
  name: {{ values.name }}
  framework: {{ values.framework }}
  description: "{{ values.description }}"

build:
  runtime: {{ values.runtime }}
  pkgMgr: {{ values.pkgMgr }}
  buildTool: {{ values.buildTool }}

compliance:
  standards: {{ values.compliance }}

security:
  auth: {{ values.auth }}

observability:
  backend: {{ values.observability }}

data:
  orm: {{ values.orm }}

deploy:
  registry: {{ values.registry }}
  method: {{ values.deployMethod }}

# Copy and run this command locally to build with selected configuration:
dockerBuild: "docker build --build-arg RUNTIME={{ values.runtime }} --build-arg COMPLIANCE={{ values.compliance }} --build-arg OBSERVABILITY={{ values.observability }} --build-arg AUTH={{ values.auth }} --build-arg ORM={{ values.orm }} --build-arg PKG_MGR={{ values.pkgMgr }} --build-arg BUILD_TOOL={{ values.buildTool }} ."
