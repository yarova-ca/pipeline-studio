# Platform build config - generated by Backstage scaffolder. Do not edit.

service:
  name: {{ values.name }}
  language: {{ values.language }}
  description: "{{ values.description }}"
  protoPackage: {{ values.protoPackage }}

compliance:
  standards: {{ values.compliance }}

security:
  auth: {{ values.auth }}

observability:
  backend: {{ values.observability }}

deploy:
  registry: {{ values.registry }}

dockerBuild: "docker build --build-arg COMPLIANCE={{ values.compliance }} --build-arg AUTH={{ values.auth }} --build-arg OBSERVABILITY={{ values.observability }} ."
