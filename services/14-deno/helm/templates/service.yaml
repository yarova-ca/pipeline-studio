apiVersion: v1
kind: Service
metadata:
  name: {{ include "14-deno.fullname" . }}
  labels:
    {{- include "14-deno.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "14-deno.selectorLabels" . | nindent 4 }}
