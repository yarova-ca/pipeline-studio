{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "14-deno.serviceAccountName" . }}
  labels:
    {{- include "14-deno.labels" . | nindent 4 }}
{{- end }}
