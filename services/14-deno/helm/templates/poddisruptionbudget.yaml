apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "14-deno.fullname" . }}
  labels:
    {{- include "14-deno.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "14-deno.selectorLabels" . | nindent 6 }}
