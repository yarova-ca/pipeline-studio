{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "01-nextjs.serviceAccountName" . }}
  labels:
    {{- include "01-nextjs.labels" . | nindent 4 }}
{{- end }}
