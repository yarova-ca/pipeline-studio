{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "01-nextjs.fullname" . }}
  labels:
    {{- include "01-nextjs.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "01-nextjs.selectorLabels" . | nindent 6 }}
{{- end }}
