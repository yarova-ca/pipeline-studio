apiVersion: v1
kind: Service
metadata:
  name: {{ include "23-symfony.fullname" . }}
  labels:
    {{- include "23-symfony.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "23-symfony.selectorLabels" . | nindent 4 }}
