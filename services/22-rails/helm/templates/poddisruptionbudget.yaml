{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "22-rails.fullname" . }}
  labels:
    {{- include "22-rails.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "22-rails.selectorLabels" . | nindent 6 }}
{{- end }}
