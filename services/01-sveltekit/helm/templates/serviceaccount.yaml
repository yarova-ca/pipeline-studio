{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "01-sveltekit.serviceAccountName" . }}
  labels:
    {{- include "01-sveltekit.labels" . | nindent 4 }}
{{- end }}
