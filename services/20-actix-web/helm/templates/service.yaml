apiVersion: v1
kind: Service
metadata:
  name: {{ include "20-actix-web.fullname" . }}
  labels:
    {{- include "20-actix-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "20-actix-web.selectorLabels" . | nindent 4 }}
