{
  "name": "web-react-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node -e \"if(!process.env.PUBLIC_APP_NAME){console.error('FATAL: PUBLIC_APP_NAME must be set at build');process.exit(1)}\"",
    "build": "vite build",
    "dev": "vite",
    "start": "node server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
