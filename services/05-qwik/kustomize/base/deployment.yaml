apiVersion: apps/v1
kind: Deployment
metadata:
  name: 05-qwik
  labels:
    app: 05-qwik
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 05-qwik
  template:
    metadata:
      labels:
        app: 05-qwik
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 05-qwik
          image: ghcr.io/yarova-ca/05-qwik:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
