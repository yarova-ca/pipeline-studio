apiVersion: v1
kind: Service
metadata:
  name: {{ include "05-qwik.fullname" . }}
  labels:
    {{- include "05-qwik.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "05-qwik.selectorLabels" . | nindent 4 }}
