{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "05-qwik.fullname" . }}
  labels:
    {{- include "05-qwik.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "05-qwik.selectorLabels" . | nindent 6 }}
{{- end }}
