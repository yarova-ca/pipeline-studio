apiVersion: apps/v1
kind: Deployment
metadata:
  name: 20-axum
  labels:
    app: 20-axum
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 20-axum
  template:
    metadata:
      labels:
        app: 20-axum
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 20-axum
          image: ghcr.io/yarova-ca/20-axum:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
