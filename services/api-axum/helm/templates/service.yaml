apiVersion: v1
kind: Service
metadata:
  name: {{ include "20-axum.fullname" . }}
  labels:
    {{- include "20-axum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "20-axum.selectorLabels" . | nindent 4 }}
