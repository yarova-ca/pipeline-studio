{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "20-axum.serviceAccountName" . }}
  labels:
    {{- include "20-axum.labels" . | nindent 4 }}
{{- end }}
