{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "02-svelte.serviceAccountName" . }}
  labels:
    {{- include "02-svelte.labels" . | nindent 4 }}
{{- end }}
