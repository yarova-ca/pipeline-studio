{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "04-fresh.serviceAccountName" . }}
  labels:
    {{- include "04-fresh.labels" . | nindent 4 }}
{{- end }}
