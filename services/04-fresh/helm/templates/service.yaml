apiVersion: v1
kind: Service
metadata:
  name: {{ include "04-fresh.fullname" . }}
  labels:
    {{- include "04-fresh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "04-fresh.selectorLabels" . | nindent 4 }}
