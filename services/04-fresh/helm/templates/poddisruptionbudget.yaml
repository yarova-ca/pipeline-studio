apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "04-fresh.fullname" . }}
  labels:
    {{- include "04-fresh.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "04-fresh.selectorLabels" . | nindent 6 }}
