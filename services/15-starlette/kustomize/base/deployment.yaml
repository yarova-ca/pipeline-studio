apiVersion: apps/v1
kind: Deployment
metadata:
  name: 15-starlette
  labels:
    app: 15-starlette
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 15-starlette
  template:
    metadata:
      labels:
        app: 15-starlette
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 15-starlette
          image: ghcr.io/yarova-ca/15-starlette:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
