{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "15-starlette.fullname" . }}
  labels:
    {{- include "15-starlette.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "15-starlette.selectorLabels" . | nindent 6 }}
{{- end }}
