apiVersion: v1
kind: Service
metadata:
  name: {{ include "15-starlette.fullname" . }}
  labels:
    {{- include "15-starlette.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "15-starlette.selectorLabels" . | nindent 4 }}
