apiVersion: v1
kind: Service
metadata:
  name: {{ include "23-slim.fullname" . }}
  labels:
    {{- include "23-slim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "23-slim.selectorLabels" . | nindent 4 }}
