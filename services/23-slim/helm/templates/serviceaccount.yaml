{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "23-slim.serviceAccountName" . }}
  labels:
    {{- include "23-slim.labels" . | nindent 4 }}
{{- end }}
