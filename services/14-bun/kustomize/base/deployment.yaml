apiVersion: apps/v1
kind: Deployment
metadata:
  name: 14-bun
  labels:
    app: 14-bun
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 14-bun
  template:
    metadata:
      labels:
        app: 14-bun
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 14-bun
          image: ghcr.io/yarova-ca/14-bun:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
