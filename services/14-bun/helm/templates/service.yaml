apiVersion: v1
kind: Service
metadata:
  name: {{ include "14-bun.fullname" . }}
  labels:
    {{- include "14-bun.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "14-bun.selectorLabels" . | nindent 4 }}
