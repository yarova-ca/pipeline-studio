apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "14-bun.fullname" . }}
  labels:
    {{- include "14-bun.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "14-bun.selectorLabels" . | nindent 6 }}
