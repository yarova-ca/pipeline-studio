{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "14-bun.serviceAccountName" . }}
  labels:
    {{- include "14-bun.labels" . | nindent 4 }}
{{- end }}
