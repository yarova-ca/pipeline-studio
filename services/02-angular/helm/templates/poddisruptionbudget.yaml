{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "02-angular.fullname" . }}
  labels:
    {{- include "02-angular.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "02-angular.selectorLabels" . | nindent 6 }}
{{- end }}
