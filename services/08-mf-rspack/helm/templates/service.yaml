apiVersion: v1
kind: Service
metadata:
  name: {{ include "08-mf-rspack.fullname" . }}
  labels:
    {{- include "08-mf-rspack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "08-mf-rspack.selectorLabels" . | nindent 4 }}
