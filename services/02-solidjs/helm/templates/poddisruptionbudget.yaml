apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "02-solidjs.fullname" . }}
  labels:
    {{- include "02-solidjs.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "02-solidjs.selectorLabels" . | nindent 6 }}
