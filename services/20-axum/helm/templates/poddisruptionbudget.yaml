apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "20-axum.fullname" . }}
  labels:
    {{- include "20-axum.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "20-axum.selectorLabels" . | nindent 6 }}
