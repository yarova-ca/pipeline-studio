{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "19-minimal-apis.serviceAccountName" . }}
  labels:
    {{- include "19-minimal-apis.labels" . | nindent 4 }}
{{- end }}
