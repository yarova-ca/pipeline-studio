apiVersion: v1
kind: Service
metadata:
  name: {{ include "19-minimal-apis.fullname" . }}
  labels:
    {{- include "19-minimal-apis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "19-minimal-apis.selectorLabels" . | nindent 4 }}
