apiVersion: v1
kind: Service
metadata:
  name: {{ include "16-gin.fullname" . }}
  labels:
    {{- include "16-gin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "16-gin.selectorLabels" . | nindent 4 }}
