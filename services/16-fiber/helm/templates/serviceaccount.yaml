{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "16-fiber.serviceAccountName" . }}
  labels:
    {{- include "16-fiber.labels" . | nindent 4 }}
{{- end }}
