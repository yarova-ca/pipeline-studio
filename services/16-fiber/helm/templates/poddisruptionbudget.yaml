{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "16-fiber.fullname" . }}
  labels:
    {{- include "16-fiber.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "16-fiber.selectorLabels" . | nindent 6 }}
{{- end }}
