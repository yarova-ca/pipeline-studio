apiVersion: apps/v1
kind: Deployment
metadata:
  name: 16-fiber
  labels:
    app: 16-fiber
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 16-fiber
  template:
    metadata:
      labels:
        app: 16-fiber
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 16-fiber
          image: ghcr.io/yarova-ca/16-fiber:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
