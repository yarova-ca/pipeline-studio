apiVersion: v1
kind: Service
metadata:
  name: {{ include "25-http4s.fullname" . }}
  labels:
    {{- include "25-http4s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "25-http4s.selectorLabels" . | nindent 4 }}
