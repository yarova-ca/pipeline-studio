apiVersion: apps/v1
kind: Deployment
metadata:
  name: 25-http4s
  labels:
    app: 25-http4s
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 25-http4s
  template:
    metadata:
      labels:
        app: 25-http4s
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 25-http4s
          image: ghcr.io/yarova-ca/25-http4s:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
