apiVersion: v1
kind: Service
metadata:
  name: {{ include "18-spring-boot-kotlin.fullname" . }}
  labels:
    {{- include "18-spring-boot-kotlin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "18-spring-boot-kotlin.selectorLabels" . | nindent 4 }}
