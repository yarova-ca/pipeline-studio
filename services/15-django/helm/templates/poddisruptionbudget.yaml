apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "15-django.fullname" . }}
  labels:
    {{- include "15-django.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "15-django.selectorLabels" . | nindent 6 }}
