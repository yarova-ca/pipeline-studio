apiVersion: apps/v1
kind: Deployment
metadata:
  name: 28-go-grpc
  labels:
    app: 28-go-grpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 28-go-grpc
  template:
    metadata:
      labels:
        app: 28-go-grpc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 28-go-grpc
          image: ghcr.io/yarova-ca/28-go-grpc:latest
          ports:
            - containerPort: 50051
          livenessProbe:
            httpGet:
              path: /health/live
              port: 50051
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 50051
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
