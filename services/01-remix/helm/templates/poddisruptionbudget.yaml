{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "01-remix.fullname" . }}
  labels:
    {{- include "01-remix.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "01-remix.selectorLabels" . | nindent 6 }}
{{- end }}
