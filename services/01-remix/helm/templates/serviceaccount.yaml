{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "01-remix.serviceAccountName" . }}
  labels:
    {{- include "01-remix.labels" . | nindent 4 }}
{{- end }}
