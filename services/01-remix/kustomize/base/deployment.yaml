apiVersion: apps/v1
kind: Deployment
metadata:
  name: 01-remix
  labels:
    app: 01-remix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 01-remix
  template:
    metadata:
      labels:
        app: 01-remix
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 01-remix
          image: ghcr.io/yarova-ca/01-remix:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
