apiVersion: apps/v1
kind: Deployment
metadata:
  name: 14-express
  labels:
    app: 14-express
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 14-express
  template:
    metadata:
      labels:
        app: 14-express
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 14-express
          image: ghcr.io/yarova-ca/14-express:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
