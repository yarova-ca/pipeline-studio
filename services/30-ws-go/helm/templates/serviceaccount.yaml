{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "30-ws-go.serviceAccountName" . }}
  labels:
    {{- include "30-ws-go.labels" . | nindent 4 }}
{{- end }}
