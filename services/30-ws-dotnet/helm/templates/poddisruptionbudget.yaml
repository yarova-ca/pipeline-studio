apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "30-ws-dotnet.fullname" . }}
  labels:
    {{- include "30-ws-dotnet.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "30-ws-dotnet.selectorLabels" . | nindent 6 }}
