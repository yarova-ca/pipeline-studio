apiVersion: v1
kind: Service
metadata:
  name: {{ include "30-ws-dotnet.fullname" . }}
  labels:
    {{- include "30-ws-dotnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "30-ws-dotnet.selectorLabels" . | nindent 4 }}
