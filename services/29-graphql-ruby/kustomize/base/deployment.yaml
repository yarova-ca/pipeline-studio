apiVersion: apps/v1
kind: Deployment
metadata:
  name: 29-graphql-ruby
  labels:
    app: 29-graphql-ruby
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 29-graphql-ruby
  template:
    metadata:
      labels:
        app: 29-graphql-ruby
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 29-graphql-ruby
          image: ghcr.io/yarova-ca/29-graphql-ruby:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
