apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "30-ws-rust.fullname" . }}
  labels:
    {{- include "30-ws-rust.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "30-ws-rust.selectorLabels" . | nindent 6 }}
