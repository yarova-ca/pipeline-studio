{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "30-ws-rust.serviceAccountName" . }}
  labels:
    {{- include "30-ws-rust.labels" . | nindent 4 }}
{{- end }}
