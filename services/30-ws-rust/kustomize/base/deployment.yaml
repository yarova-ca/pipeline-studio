apiVersion: apps/v1
kind: Deployment
metadata:
  name: 30-ws-rust
  labels:
    app: 30-ws-rust
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 30-ws-rust
  template:
    metadata:
      labels:
        app: 30-ws-rust
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 30-ws-rust
          image: ghcr.io/yarova-ca/30-ws-rust:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
