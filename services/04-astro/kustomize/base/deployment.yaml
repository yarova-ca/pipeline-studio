apiVersion: apps/v1
kind: Deployment
metadata:
  name: 04-astro
  labels:
    app: 04-astro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 04-astro
  template:
    metadata:
      labels:
        app: 04-astro
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 04-astro
          image: ghcr.io/yarova-ca/04-astro:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
