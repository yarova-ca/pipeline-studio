apiVersion: v1
kind: Service
metadata:
  name: {{ include "13-workbox.fullname" . }}
  labels:
    {{- include "13-workbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "13-workbox.selectorLabels" . | nindent 4 }}
