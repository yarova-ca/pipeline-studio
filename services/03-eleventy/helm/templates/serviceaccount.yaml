{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "03-eleventy.serviceAccountName" . }}
  labels:
    {{- include "03-eleventy.labels" . | nindent 4 }}
{{- end }}
