apiVersion: v1
kind: Service
metadata:
  name: {{ include "17-spring-boot.fullname" . }}
  labels:
    {{- include "17-spring-boot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "17-spring-boot.selectorLabels" . | nindent 4 }}
