apiVersion: v1
kind: Service
metadata:
  name: {{ include "28-php-grpc.fullname" . }}
  labels:
    {{- include "28-php-grpc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "28-php-grpc.selectorLabels" . | nindent 4 }}
