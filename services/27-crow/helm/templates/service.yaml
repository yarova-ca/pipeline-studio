apiVersion: v1
kind: Service
metadata:
  name: {{ include "27-crow.fullname" . }}
  labels:
    {{- include "27-crow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "27-crow.selectorLabels" . | nindent 4 }}
