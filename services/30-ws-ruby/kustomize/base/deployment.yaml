apiVersion: apps/v1
kind: Deployment
metadata:
  name: 30-ws-ruby
  labels:
    app: 30-ws-ruby
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 30-ws-ruby
  template:
    metadata:
      labels:
        app: 30-ws-ruby
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 30-ws-ruby
          image: ghcr.io/yarova-ca/30-ws-ruby:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
