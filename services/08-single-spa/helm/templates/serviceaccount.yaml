{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "08-single-spa.serviceAccountName" . }}
  labels:
    {{- include "08-single-spa.labels" . | nindent 4 }}
{{- end }}
