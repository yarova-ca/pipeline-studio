apiVersion: v1
kind: Service
metadata:
  name: {{ include "08-single-spa.fullname" . }}
  labels:
    {{- include "08-single-spa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "08-single-spa.selectorLabels" . | nindent 4 }}
