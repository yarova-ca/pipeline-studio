apiVersion: apps/v1
kind: Deployment
metadata:
  name: 24-vapor
  labels:
    app: 24-vapor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 24-vapor
  template:
    metadata:
      labels:
        app: 24-vapor
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 24-vapor
          image: ghcr.io/yarova-ca/24-vapor:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
