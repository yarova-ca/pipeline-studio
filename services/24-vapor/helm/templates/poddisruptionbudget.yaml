apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "24-vapor.fullname" . }}
  labels:
    {{- include "24-vapor.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "24-vapor.selectorLabels" . | nindent 6 }}
