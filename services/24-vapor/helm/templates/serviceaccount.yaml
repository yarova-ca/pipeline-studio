{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "24-vapor.serviceAccountName" . }}
  labels:
    {{- include "24-vapor.labels" . | nindent 4 }}
{{- end }}
