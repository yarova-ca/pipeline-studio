apiVersion: v1
kind: Service
metadata:
  name: {{ include "24-vapor.fullname" . }}
  labels:
    {{- include "24-vapor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "24-vapor.selectorLabels" . | nindent 4 }}
