apiVersion: v1
kind: Service
metadata:
  name: {{ include "07-nextjs-app-router.fullname" . }}
  labels:
    {{- include "07-nextjs-app-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "07-nextjs-app-router.selectorLabels" . | nindent 4 }}
