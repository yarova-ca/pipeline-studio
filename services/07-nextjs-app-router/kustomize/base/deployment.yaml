apiVersion: apps/v1
kind: Deployment
metadata:
  name: 07-nextjs-app-router
  labels:
    app: 07-nextjs-app-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 07-nextjs-app-router
  template:
    metadata:
      labels:
        app: 07-nextjs-app-router
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 07-nextjs-app-router
          image: ghcr.io/yarova-ca/07-nextjs-app-router:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
