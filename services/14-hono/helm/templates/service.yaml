apiVersion: v1
kind: Service
metadata:
  name: {{ include "14-hono.fullname" . }}
  labels:
    {{- include "14-hono.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "14-hono.selectorLabels" . | nindent 4 }}
