{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "14-hono.serviceAccountName" . }}
  labels:
    {{- include "14-hono.labels" . | nindent 4 }}
{{- end }}
