apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "14-hono.fullname" . }}
  labels:
    {{- include "14-hono.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "14-hono.selectorLabels" . | nindent 6 }}
