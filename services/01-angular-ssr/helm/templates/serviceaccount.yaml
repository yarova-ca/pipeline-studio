{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "01-angular-ssr.serviceAccountName" . }}
  labels:
    {{- include "01-angular-ssr.labels" . | nindent 4 }}
{{- end }}
