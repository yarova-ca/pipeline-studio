apiVersion: apps/v1
kind: Deployment
metadata:
  name: 03-hugo
  labels:
    app: 03-hugo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 03-hugo
  template:
    metadata:
      labels:
        app: 03-hugo
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 03-hugo
          image: ghcr.io/yarova-ca/03-hugo:latest
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /health/live
              port: 80
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 80
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
