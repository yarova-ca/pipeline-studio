{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "03-hugo.serviceAccountName" . }}
  labels:
    {{- include "03-hugo.labels" . | nindent 4 }}
{{- end }}
