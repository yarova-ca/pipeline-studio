apiVersion: v1
kind: Service
metadata:
  name: {{ include "03-hugo.fullname" . }}
  labels:
    {{- include "03-hugo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "03-hugo.selectorLabels" . | nindent 4 }}
