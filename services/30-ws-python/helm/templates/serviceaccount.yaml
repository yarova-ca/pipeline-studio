{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "30-ws-python.serviceAccountName" . }}
  labels:
    {{- include "30-ws-python.labels" . | nindent 4 }}
{{- end }}
