{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "25-play.serviceAccountName" . }}
  labels:
    {{- include "25-play.labels" . | nindent 4 }}
{{- end }}
