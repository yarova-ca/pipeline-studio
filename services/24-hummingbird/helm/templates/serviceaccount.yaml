{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "24-hummingbird.serviceAccountName" . }}
  labels:
    {{- include "24-hummingbird.labels" . | nindent 4 }}
{{- end }}
