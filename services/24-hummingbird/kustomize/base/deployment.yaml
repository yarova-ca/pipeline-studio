apiVersion: apps/v1
kind: Deployment
metadata:
  name: 24-hummingbird
  labels:
    app: 24-hummingbird
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 24-hummingbird
  template:
    metadata:
      labels:
        app: 24-hummingbird
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 24-hummingbird
          image: ghcr.io/yarova-ca/24-hummingbird:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
