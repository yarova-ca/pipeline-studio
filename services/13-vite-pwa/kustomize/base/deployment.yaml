apiVersion: apps/v1
kind: Deployment
metadata:
  name: 13-vite-pwa
  labels:
    app: 13-vite-pwa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 13-vite-pwa
  template:
    metadata:
      labels:
        app: 13-vite-pwa
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 13-vite-pwa
          image: ghcr.io/yarova-ca/13-vite-pwa:latest
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /health/live
              port: 80
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 80
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
