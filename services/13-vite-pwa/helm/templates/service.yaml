apiVersion: v1
kind: Service
metadata:
  name: {{ include "13-vite-pwa.fullname" . }}
  labels:
    {{- include "13-vite-pwa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "13-vite-pwa.selectorLabels" . | nindent 4 }}
