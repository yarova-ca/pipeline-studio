apiVersion: apps/v1
kind: Deployment
metadata:
  name: 28-dotnet-grpc
  labels:
    app: 28-dotnet-grpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 28-dotnet-grpc
  template:
    metadata:
      labels:
        app: 28-dotnet-grpc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 28-dotnet-grpc
          image: ghcr.io/yarova-ca/28-dotnet-grpc:latest
          ports:
            - containerPort: 5000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 5000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 5000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
