apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "15-fastapi.fullname" . }}
  labels:
    {{- include "15-fastapi.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "15-fastapi.selectorLabels" . | nindent 6 }}
