apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "29-apollo.fullname" . }}
  labels:
    {{- include "29-apollo.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "29-apollo.selectorLabels" . | nindent 6 }}
