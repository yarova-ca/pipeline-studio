{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "29-apollo.serviceAccountName" . }}
  labels:
    {{- include "29-apollo.labels" . | nindent 4 }}
{{- end }}
