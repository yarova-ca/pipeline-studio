{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "02-preact.fullname" . }}
  labels:
    {{- include "02-preact.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "02-preact.selectorLabels" . | nindent 6 }}
{{- end }}
