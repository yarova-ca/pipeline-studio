apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "23-laravel.fullname" . }}
  labels:
    {{- include "23-laravel.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "23-laravel.selectorLabels" . | nindent 6 }}
