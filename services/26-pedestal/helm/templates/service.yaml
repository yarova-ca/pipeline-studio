apiVersion: v1
kind: Service
metadata:
  name: {{ include "26-pedestal.fullname" . }}
  labels:
    {{- include "26-pedestal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "26-pedestal.selectorLabels" . | nindent 4 }}
