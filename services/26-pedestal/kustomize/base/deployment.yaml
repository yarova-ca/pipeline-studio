apiVersion: apps/v1
kind: Deployment
metadata:
  name: 26-pedestal
  labels:
    app: 26-pedestal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 26-pedestal
  template:
    metadata:
      labels:
        app: 26-pedestal
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 26-pedestal
          image: ghcr.io/yarova-ca/26-pedestal:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
