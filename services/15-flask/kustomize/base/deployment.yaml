apiVersion: apps/v1
kind: Deployment
metadata:
  name: 15-flask
  labels:
    app: 15-flask
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 15-flask
  template:
    metadata:
      labels:
        app: 15-flask
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 15-flask
          image: ghcr.io/yarova-ca/15-flask:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
