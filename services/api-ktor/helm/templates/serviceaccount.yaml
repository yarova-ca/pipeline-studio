{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "18-ktor.serviceAccountName" . }}
  labels:
    {{- include "18-ktor.labels" . | nindent 4 }}
{{- end }}
