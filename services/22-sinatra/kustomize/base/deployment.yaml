apiVersion: apps/v1
kind: Deployment
metadata:
  name: 22-sinatra
  labels:
    app: 22-sinatra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 22-sinatra
  template:
    metadata:
      labels:
        app: 22-sinatra
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 22-sinatra
          image: ghcr.io/yarova-ca/22-sinatra:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
