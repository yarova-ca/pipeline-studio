replicaCount: 1

image:
  repository: "ghcr.io/yarova-ca/22-sinatra"   # set in CI: ghcr.io/yarova-ca/22-sinatra
  pullPolicy: IfNotPresent
  tag: "latest"

nameOverride: ""
fullnameOverride: "22-sinatra"

serviceAccount:
  create: true
  name: "22-sinatra"

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: 22-sinatra.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

env: {}

envFrom: []

livenessProbe:
  httpGet:
    path: /health/live
    port: 3000
  initialDelaySeconds: 10
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health/ready
    port: 3000
  initialDelaySeconds: 5
  periodSeconds: 5

networkPolicy:
  enabled: false
