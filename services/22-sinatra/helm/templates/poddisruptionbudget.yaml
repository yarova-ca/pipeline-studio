{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "22-sinatra.fullname" . }}
  labels:
    {{- include "22-sinatra.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "22-sinatra.selectorLabels" . | nindent 6 }}
{{- end }}
