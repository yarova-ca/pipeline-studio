fastapi==0.137.1
uvicorn[standard]==0.49.0
python-dotenv==1.2.2
# ORM + migrations
sqlalchemy==2.0.51
alembic==1.18.4
asyncpg==0.31.0
aiosqlite==0.22.1
# Auth
python-jose[cryptography]==3.5.0
# OAuth HTTP calls
httpx==0.28.1
# Structured logging
structlog==26.1.0
# Rate limiting
slowapi==0.1.10
# OpenTelemetry — guarded by OTEL_ENABLED=true in main.py
# API/SDK/exporter are pinned to the latest stable 1.42.1.
# The instrumentation package has no stable line — OTel ships it only as 0.XXbN.
# 0.63b1 is the current release and requires opentelemetry-api~=1.12 (1.42.1 OK).
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-grpc==1.42.1
opentelemetry-instrumentation-fastapi==0.63b1
# Testing
pytest==9.1.0
pytest-asyncio==1.4.0
prometheus-client==0.25.0
