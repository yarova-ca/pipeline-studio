apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "01-nuxt.fullname" . }}
  labels:
    {{- include "01-nuxt.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "01-nuxt.selectorLabels" . | nindent 6 }}
