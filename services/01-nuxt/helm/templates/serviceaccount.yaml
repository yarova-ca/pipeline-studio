{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "01-nuxt.serviceAccountName" . }}
  labels:
    {{- include "01-nuxt.labels" . | nindent 4 }}
{{- end }}
