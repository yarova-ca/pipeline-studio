apiVersion: apps/v1
kind: Deployment
metadata:
  name: 01-nuxt
  labels:
    app: 01-nuxt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 01-nuxt
  template:
    metadata:
      labels:
        app: 01-nuxt
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 01-nuxt
          image: ghcr.io/yarova-ca/01-nuxt:latest
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
