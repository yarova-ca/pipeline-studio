apiVersion: v1
kind: Service
metadata:
  name: {{ include "02-react.fullname" . }}
  labels:
    {{- include "02-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "02-react.selectorLabels" . | nindent 4 }}
