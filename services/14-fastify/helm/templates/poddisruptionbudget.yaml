{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "14-fastify.fullname" . }}
  labels:
    {{- include "14-fastify.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "14-fastify.selectorLabels" . | nindent 6 }}
{{- end }}
