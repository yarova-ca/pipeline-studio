{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "14-fastify.serviceAccountName" . }}
  labels:
    {{- include "14-fastify.labels" . | nindent 4 }}
{{- end }}
