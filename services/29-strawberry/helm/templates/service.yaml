apiVersion: v1
kind: Service
metadata:
  name: {{ include "29-strawberry.fullname" . }}
  labels:
    {{- include "29-strawberry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "29-strawberry.selectorLabels" . | nindent 4 }}
