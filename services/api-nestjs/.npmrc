# @nestjs/swagger@8 declares a peer range of NestJS 9/10, but runs fine on
# NestJS 11 (this project's pinned major). Accept the stale peer range so
# `npm ci` / `npm install` resolve cleanly in CI.
legacy-peer-deps=true
