{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "14-nestjs.fullname" . }}
  labels:
    {{- include "14-nestjs.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "14-nestjs.selectorLabels" . | nindent 6 }}
{{- end }}
