{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "14-nestjs.serviceAccountName" . }}
  labels:
    {{- include "14-nestjs.labels" . | nindent 4 }}
{{- end }}
