{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "17-micronaut.serviceAccountName" . }}
  labels:
    {{- include "17-micronaut.labels" . | nindent 4 }}
{{- end }}
