{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "17-micronaut.fullname" . }}
  labels:
    {{- include "17-micronaut.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "17-micronaut.selectorLabels" . | nindent 6 }}
{{- end }}
