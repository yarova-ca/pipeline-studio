apiVersion: v1
kind: Service
metadata:
  name: {{ include "17-micronaut.fullname" . }}
  labels:
    {{- include "17-micronaut.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "17-micronaut.selectorLabels" . | nindent 4 }}
