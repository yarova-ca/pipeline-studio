apiVersion: apps/v1
kind: Deployment
metadata:
  name: 17-micronaut
  labels:
    app: 17-micronaut
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 17-micronaut
  template:
    metadata:
      labels:
        app: 17-micronaut
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 17-micronaut
          image: ghcr.io/yarova-ca/17-micronaut:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
