{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "27-drogon.fullname" . }}
  labels:
    {{- include "27-drogon.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "27-drogon.selectorLabels" . | nindent 6 }}
{{- end }}
