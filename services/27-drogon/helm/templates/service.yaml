apiVersion: v1
kind: Service
metadata:
  name: {{ include "27-drogon.fullname" . }}
  labels:
    {{- include "27-drogon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "27-drogon.selectorLabels" . | nindent 4 }}
