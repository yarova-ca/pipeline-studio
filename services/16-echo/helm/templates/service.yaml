apiVersion: v1
kind: Service
metadata:
  name: {{ include "16-echo.fullname" . }}
  labels:
    {{- include "16-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "16-echo.selectorLabels" . | nindent 4 }}
