{{- if ge (int .Values.replicaCount) 2 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "21-phoenix.fullname" . }}
  labels:
    {{- include "21-phoenix.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "21-phoenix.selectorLabels" . | nindent 6 }}
{{- end }}
