apiVersion: v1
kind: Service
metadata:
  name: {{ include "21-phoenix.fullname" . }}
  labels:
    {{- include "21-phoenix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "21-phoenix.selectorLabels" . | nindent 4 }}
