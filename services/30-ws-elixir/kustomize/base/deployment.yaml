apiVersion: apps/v1
kind: Deployment
metadata:
  name: 30-ws-elixir
  labels:
    app: 30-ws-elixir
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 30-ws-elixir
  template:
    metadata:
      labels:
        app: 30-ws-elixir
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: 30-ws-elixir
          image: ghcr.io/yarova-ca/30-ws-elixir:latest
          ports:
            - containerPort: 4000
          livenessProbe:
            httpGet:
              path: /health/live
              port: 4000
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 4000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
