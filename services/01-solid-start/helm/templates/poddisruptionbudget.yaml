apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "01-solid-start.fullname" . }}
  labels:
    {{- include "01-solid-start.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "01-solid-start.selectorLabels" . | nindent 6 }}
