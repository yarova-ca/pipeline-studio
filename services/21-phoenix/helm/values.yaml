replicaCount: 1

image:
  repository: ""   # set in CI: ghcr.io/yarova-ca/21-phoenix
  pullPolicy: IfNotPresent
  tag: "latest"

nameOverride: ""
fullnameOverride: "21-phoenix"

serviceAccount:
  create: true
  name: "21-phoenix"

service:
  type: ClusterIP
  port: 4000

ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: 21-phoenix.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

env: {}

envFrom: []

livenessProbe:
  httpGet:
    path: /health/live
    port: 4000
  initialDelaySeconds: 10
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health/ready
    port: 4000
  initialDelaySeconds: 5
  periodSeconds: 5
