apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "02-vue.fullname" . }}
  labels:
    {{- include "02-vue.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "02-vue.selectorLabels" . | nindent 6 }}
