{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "02-vue.serviceAccountName" . }}
  labels:
    {{- include "02-vue.labels" . | nindent 4 }}
{{- end }}
