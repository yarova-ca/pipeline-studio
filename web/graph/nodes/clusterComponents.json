[
  {
    "id": "comp-01-namespaces-resourcequota-limitrange",
    "num": "01",
    "name": "Namespaces + ResourceQuota + LimitRange",
    "label": "Namespaces + ResourceQuota + LimitRange",
    "layer": "Foundation",
    "status": "EXISTS",
    "what": "Creates isolated environments (dev/staging/prod) and caps how much CPU/memory all pods in each namespace can consume.",
    "why": "Without ResourceQuota, one misconfigured service can consume all cluster CPU and starve every other service. LimitRange sets default requests/limits on pods that don't specify them.",
    "installCommand": "kubectl apply -f infra/namespaces.yaml\nkubectl apply -f infra/namespace-quotas.yaml",
    "wiredTo": "Every other component deploys into a namespace. StorageClass quotas affect PostgreSQL and Redis. ResourceQuota is enforced alongside Kyverno require-resource-limits policy.",
    "verifyCommand": "kubectl get resourcequota -n prod",
    "verifyExpected": "Expected: prod-quota with limits.cpu: 40 and limits.memory: 80Gi",
    "failures": [
      {
        "trigger": "ResourceQuota missing from namespace",
        "system": "A service requests unlimited CPU; scheduler grants it; starves other services",
        "userSees": "Other services become unresponsive; Prometheus alerts fire for latency spikes",
        "userCan": "kubectl apply -f infra/namespace-quotas.yaml; then restart starving services"
      }
    ],
    "decision": {
      "chosen": "Decision: ResourceQuota per namespace, not per deployment",
      "rejected": "Rejected: Resource limits only in Helm values — individual deployments can be changed; namespace quota cannot be bypassed by a single service",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-02-storageclass",
    "num": "02",
    "name": "StorageClass",
    "label": "StorageClass",
    "layer": "Foundation",
    "status": "EXISTS",
    "what": "Defines how Kubernetes dynamically provisions persistent disks for PVCs (PersistentVolumeClaims).",
    "why": "Without a default StorageClass, any PVC stays in Pending state indefinitely. PostgreSQL, Redis, and Loki all require persistent volumes.",
    "installCommand": "kubectl apply -f infra/storage-class.yaml",
    "installNote": "Cloud-specific: EKS uses gp3, GKE uses pd-ssd, AKS uses managed-premium.",
    "wiredTo": "PostgreSQL (CNPG), Redis (Sentinel), Loki, Tempo all claim PVCs. Must exist before any of those are installed.",
    "verifyCommand": "kubectl get storageclass",
    "verifyExpected": "Expected: at least one StorageClass with (default) annotation",
    "failures": [
      {
        "trigger": "No default StorageClass exists when a PVC is created",
        "system": "PVC status stays Pending; pods that depend on it stay in ContainerCreating",
        "userSees": "kubectl get pods shows PostgreSQL, Redis, Loki pods stuck in ContainerCreating",
        "userCan": "kubectl apply -f infra/storage-class.yaml; PVC binds automatically within 30s"
      }
    ],
    "decision": {
      "chosen": "Decision: gp3 (EKS) / pd-ssd (GKE) — SSD-backed storage class as default",
      "rejected": "Rejected: gp2 (EKS) — older generation, lower throughput, higher latency for database workloads",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-03-rbac-clusterroles-rolebindings-serviceaccounts",
    "num": "03",
    "name": "RBAC — ClusterRoles, RoleBindings, ServiceAccounts",
    "label": "RBAC — ClusterRoles, RoleBindings, ServiceAccounts",
    "layer": "Foundation",
    "status": "EXISTS",
    "what": "Grants minimum necessary permissions to each team (dev read-only, ops deploy, CI service account) and to each service's ServiceAccount.",
    "why": "Without RBAC, every team member has cluster-admin by default — a compromised CI token can delete all namespaces.",
    "installCommand": "kubectl apply -f infra/rbac/service-accounts.yaml\nkubectl apply -f infra/rbac/roles.yaml\nkubectl apply -f infra/rbac/bindings.yaml",
    "wiredTo": "ArgoCD uses its own ServiceAccount with deploy permissions. ESO uses a ServiceAccount with secret-read permissions. Kyverno webhook uses a ClusterRole.",
    "verifyCommand": "kubectl auth can-i create pods --as=system:serviceaccount:ci:ci-runner -n prod",
    "verifyExpected": "Expected: no (CI runner must not create pods directly in prod)",
    "failures": [
      {
        "trigger": "ServiceAccount missing for a component (e.g., ESO, ArgoCD)",
        "system": "The component's pod starts but immediately fails API calls with 403 Forbidden",
        "userSees": "ESO logs show \"forbidden: User cannot get secret\"; secrets never sync",
        "userCan": "kubectl apply -f infra/rbac/ to recreate all roles and bindings; restart affected pods"
      }
    ],
    "decision": {
      "chosen": "Decision: Namespace-scoped Roles over ClusterRoles wherever possible",
      "rejected": "Rejected: Single ClusterRole for all components — simpler to write, but a compromised component gains cluster-wide access",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-04-cert-manager",
    "num": "04",
    "name": "cert-manager",
    "label": "cert-manager",
    "layer": "Security",
    "status": "EXISTS",
    "what": "Automatically issues and renews TLS certificates from Let's Encrypt for every Ingress object in the cluster.",
    "why": "Without cert-manager, certificates expire silently. Manual renewal across 106 services × 3 environments = 315 certificates to track manually.",
    "installCommand": "helm repo add jetstack https://charts.jetstack.io\nhelm upgrade --install cert-manager jetstack/cert-manager \\\n--namespace cert-manager --create-namespace \\\n--set installCRDs=true \\\n--version v1.14.0",
    "wiredTo": "ingress-nginx references cert-manager ClusterIssuers. Every Ingress object with a tls block triggers a cert-manager Certificate resource. Must be installed before ingress-nginx.",
    "verifyCommand": "kubectl get clusterissuer letsencrypt-prod -o jsonpath='{.status.conditions[0].type}'",
    "verifyExpected": "Expected: Ready",
    "failures": [
      {
        "trigger": "cert-manager pod crashes or ClusterIssuer is missing",
        "system": "Existing certificates continue working until they expire. New certificates cannot be issued.",
        "userSees": "Browser shows \"Your connection is not private\" after certificate expiry (90 days max)",
        "userCan": "kubectl rollout restart deploy/cert-manager -n cert-manager; verify ClusterIssuer with kubectl get clusterissuer"
      }
    ],
    "decision": {
      "chosen": "Decision: Let's Encrypt via cert-manager HTTP-01 challenge",
      "rejected": "Rejected: AWS ACM / GCP-managed certificates — cloud-provider-specific, breaks portability across EKS/GKE/AKS",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-05-ingress-nginx",
    "num": "05",
    "name": "ingress-nginx",
    "label": "ingress-nginx",
    "layer": "Security",
    "status": "EXISTS",
    "what": "Runs the NGINX reverse proxy that routes all external HTTP/HTTPS traffic into the cluster based on hostname and path rules.",
    "why": "Without ingress-nginx, each service would need its own cloud load balancer — that is one load balancer per service × 3 environments = 315 load balancers.",
    "installCommand": "helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx\nhelm upgrade --install ingress-nginx ingress-nginx/ingress-nginx \\\n--namespace ingress-nginx --create-namespace \\\n--set controller.replicaCount=2 \\\n--version 4.9.1",
    "wiredTo": "cert-manager provisions TLS certs for every Ingress object. ExternalDNS creates DNS records pointing to the ingress-nginx LoadBalancer IP. All 106 services create Ingress objects referencing this controller.",
    "verifyCommand": "kubectl get svc ingress-nginx-controller -n ingress-nginx",
    "verifyExpected": "Expected: TYPE=LoadBalancer with an EXTERNAL-IP (not pending)",
    "failures": [
      {
        "trigger": "ingress-nginx controller pods crash or the LoadBalancer IP is lost",
        "system": "All external traffic to every service fails. Services still run internally.",
        "userSees": "All service URLs return connection refused or timeout",
        "userCan": "kubectl rollout restart deploy/ingress-nginx-controller -n ingress-nginx"
      }
    ],
    "decision": {
      "chosen": "Decision: ingress-nginx over cloud-native load balancers (ALB, GCP LB)",
      "rejected": "Rejected: AWS ALB Ingress Controller — AWS-only, requires IRSA, incompatible with GKE/AKS",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-06-kyverno-policies",
    "num": "06",
    "name": "Kyverno + Policies",
    "label": "Kyverno + Policies",
    "layer": "Security",
    "status": "EXISTS",
    "what": "Policy engine that validates, mutates, and generates Kubernetes resources. Blocks pods that run as root, lack resource limits, or use unscanned images.",
    "why": "Without Kyverno, every developer must manually remember to set security contexts, resource limits, and non-root users. One forgotten setting = privilege escalation vector.",
    "installCommand": "helm repo add kyverno https://kyverno.github.io/kyverno/\nhelm upgrade --install kyverno kyverno/kyverno \\\n--namespace kyverno --create-namespace \\\n--version 3.1.4\nkubectl apply -f infra/kyverno-policies/",
    "wiredTo": "Policies enforce ResourceQuota by requiring all pods to have resource limits. Trivy Operator provides image scan results that Kyverno's verify-image policy checks. Must be installed before ArgoCD deploys any workloads.",
    "verifyCommand": "kubectl get clusterpolicy",
    "verifyExpected": "Expected: disallow-root-user, require-resource-limits, disallow-privilege-escalation all in READY state",
    "failures": [
      {
        "trigger": "Kyverno webhook is unavailable when a new pod is scheduled",
        "system": "If webhook is set to Fail, all pod creation is blocked. If set to Ignore, policies are skipped.",
        "userSees": "kubectl describe pod shows \"failed calling webhook\" or pods deploy without policy enforcement",
        "userCan": "kubectl rollout restart deploy/kyverno -n kyverno; check webhook config with kubectl get validatingwebhookconfiguration"
      }
    ],
    "decision": {
      "chosen": "Decision: Kyverno over OPA/Gatekeeper",
      "rejected": "Rejected: OPA Gatekeeper — requires writing Rego; steeper learning curve; no mutation support for auto-setting missing fields",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-07-network-policies",
    "num": "07",
    "name": "Network Policies",
    "label": "Network Policies",
    "layer": "Security",
    "status": "EXISTS",
    "what": "Kubernetes NetworkPolicy resources that restrict pod-to-pod traffic — each service can only talk to the services it explicitly needs.",
    "why": "Without network policies, any compromised container can reach every database, secret store, and internal service in the cluster — flat network = complete lateral movement.",
    "installCommand": "kubectl apply -f infra/network-policies/default-deny-all.yaml\nkubectl apply -f infra/network-policies/allow-ingress-nginx.yaml\nkubectl apply -f infra/network-policies/allow-monitoring.yaml\nkubectl apply -f infra/network-policies/per-service/",
    "wiredTo": "Requires a CNI plugin that enforces NetworkPolicy (Calico, Cilium, or cloud-native VPC CNI with NetworkPolicy support). ingress-nginx must be whitelisted to reach services. Prometheus must be whitelisted to scrape metrics.",
    "verifyCommand": "kubectl run test --rm -it --image=busybox -- wget -q --timeout=3 http://postgres-service.prod.svc.cluster.local 2>&1",
    "verifyExpected": "Expected: wget: download timed out (blocked by default-deny policy)",
    "failures": [
      {
        "trigger": "CNI plugin does not support NetworkPolicy enforcement (e.g., flannel without policy support)",
        "system": "NetworkPolicy objects are accepted by the API server but silently ignored; all traffic passes",
        "userSees": "No immediate error — policies appear installed but provide zero protection",
        "userCan": "Verify CNI: kubectl get pods -n kube-system | grep calico. Replace CNI if needed (requires node drain)."
      }
    ],
    "decision": {
      "chosen": "Decision: Default-deny-all base policy with explicit allow rules per service",
      "rejected": "Rejected: Allow-all base with deny rules for sensitive services — too easy to miss a new service that should be restricted",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-08-falco",
    "num": "08",
    "name": "Falco",
    "label": "Falco",
    "layer": "Security",
    "status": "EXISTS",
    "what": "Runtime security tool that monitors kernel system calls inside containers and alerts when behavior matches known attack patterns (shell spawn, file read in /etc/shadow, network scan).",
    "why": "Kyverno and network policies prevent misconfigurations but do nothing once a container is running and compromised. Falco is the only component that detects attacks happening inside a live container.",
    "installCommand": "helm repo add falcosecurity https://falcosecurity.github.io/charts\nhelm upgrade --install falco falcosecurity/falco \\\n--namespace falco --create-namespace \\\n--set falco.grpc.enabled=true \\\n--set falco.grpc_output.enabled=true \\\n--version 3.8.7",
    "wiredTo": "Falco alerts route to Alertmanager via falco-exporter. Alertmanager sends to Slack/PagerDuty. Falco requires privileged DaemonSet access to host kernel — must coexist with Kyverno's disallow-privilege-escalation policy (falco namespace is exempted).",
    "verifyCommand": "kubectl exec -n falco $(kubectl get pod -n falco -l app=falco -o jsonpath='{.items[0].metadata.name}') -- cat /var/log/falco.log | head -5",
    "verifyExpected": "Expected: Falco version line + \"Loading rules from file /etc/falco/falco_rules.yaml\"",
    "failures": [
      {
        "trigger": "Falco DaemonSet pod crashes (kernel version mismatch with eBPF probe)",
        "system": "Runtime monitoring stops for that node; no alerts for attacks on that node",
        "userSees": "No alerts — silence, not an error. Only detected by checking DaemonSet pod health.",
        "userCan": "kubectl get daemonset falco -n falco; check driver compatibility at falco.org/docs; update driver version in Helm values"
      }
    ],
    "decision": {
      "chosen": "Decision: Falco eBPF driver over kernel module",
      "rejected": "Rejected: Falco kernel module — requires loading a kernel module on each node; blocked by managed Kubernetes (GKE, EKS) which disallow unsigned kernel modules",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-09-pod-security-admission-psa",
    "num": "09",
    "name": "Pod Security Admission (PSA)",
    "label": "Pod Security Admission (PSA)",
    "layer": "Security",
    "status": "EXISTS",
    "what": "Kubernetes built-in admission controller (no install required) that enforces pod security standards (restricted/baseline/privileged) per namespace via namespace labels.",
    "why": "PSA is a second, independent enforcement layer. Even if Kyverno is misconfigured or temporarily down, PSA still blocks privileged pods at the API server level.",
    "installCommand": "kubectl label namespace prod pod-security.kubernetes.io/enforce=restricted\nkubectl label namespace prod pod-security.kubernetes.io/warn=restricted\nkubectl label namespace prod pod-security.kubernetes.io/audit=restricted",
    "installNote": "PSA is built into Kubernetes 1.25+. No Helm chart needed.",
    "wiredTo": "Works alongside Kyverno — PSA catches violations Kyverno might miss (e.g., during Kyverno pod restart). Falco namespace must be labeled privileged to allow its DaemonSet.",
    "verifyCommand": "kubectl get namespace prod -o jsonpath='{.metadata.labels}'",
    "verifyExpected": "Expected: pod-security.kubernetes.io/enforce=restricted present in labels",
    "failures": [
      {
        "trigger": "Namespace label missing — PSA not applied",
        "system": "Privileged pods deploy without warning; PSA is opt-in, not global",
        "userSees": "No error — the pod runs as root with no indication",
        "userCan": "kubectl label namespace prod pod-security.kubernetes.io/enforce=restricted --overwrite"
      }
    ],
    "decision": {
      "chosen": "Decision: PSA restricted mode on prod, baseline on dev/test",
      "rejected": "Rejected: restricted mode everywhere — too many dev-only tools (debug containers, profilers) need elevated permissions",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-10-argocd",
    "num": "10",
    "name": "ArgoCD",
    "label": "ArgoCD",
    "layer": "GitOps",
    "status": "EXISTS",
    "what": "GitOps controller that watches Git repositories and automatically deploys changes to Kubernetes — reconciling actual cluster state with desired state in Git.",
    "why": "Without ArgoCD, every deploy requires a human running kubectl apply. With 106 services × 3 environments, that is manual work on 315 deployments.",
    "installCommand": "helm repo add argo https://argoproj.github.io/argo-helm\nhelm upgrade --install argocd argo/argo-cd \\\n--namespace argocd --create-namespace \\\n--values infra/argocd/values.yaml \\\n--version 6.7.3",
    "wiredTo": "ESO must exist before ArgoCD deploys workloads that reference ExternalSecrets. cert-manager and ingress-nginx must exist before ArgoCD deploys services with Ingress objects. GHCR pull secret must exist before ArgoCD triggers image pulls.",
    "verifyCommand": "argocd app list --server argocd.yarova.ca",
    "verifyExpected": "Expected: all applications show STATUS=Synced and HEALTH=Healthy",
    "failures": [
      {
        "trigger": "ArgoCD server pod crashes or Git repo becomes unreachable",
        "system": "Existing running services are unaffected. New commits are not deployed until ArgoCD recovers.",
        "userSees": "ArgoCD UI unreachable; no new deploys reach the cluster",
        "userCan": "kubectl rollout restart deploy/argocd-server -n argocd; verify git connectivity with argocd repo list"
      }
    ],
    "decision": {
      "chosen": "Decision: ArgoCD over Flux v2",
      "rejected": "Rejected: Flux v2 — no built-in UI, harder to debug sync failures for non-Kubernetes-expert developers",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-11-argo-rollouts",
    "num": "11",
    "name": "Argo Rollouts",
    "label": "Argo Rollouts",
    "layer": "GitOps",
    "status": "EXISTS",
    "what": "Extends Kubernetes Deployments with canary and blue-green release strategies — sends a small percentage of traffic to the new version and auto-rolls back if error rate or latency exceeds thresholds.",
    "why": "Standard Kubernetes Deployment sends 100% of traffic to the new version immediately. One bad deploy = complete outage. Argo Rollouts limits blast radius to the canary percentage.",
    "installCommand": "helm upgrade --install argo-rollouts argo/argo-rollouts \\\n--namespace argo-rollouts --create-namespace \\\n--version 2.35.1",
    "wiredTo": "Uses Prometheus metrics to make auto-rollback decisions — requires Prometheus to be running and scraping the target service. ArgoCD deploys Rollout objects instead of standard Deployment objects.",
    "verifyCommand": "kubectl argo rollouts get rollout <service-name> -n prod --watch",
    "verifyExpected": "Expected: shows canary weight progression and Healthy status",
    "failures": [
      {
        "trigger": "Argo Rollouts controller pod crashes during an active canary",
        "system": "The canary stops progressing; traffic split freezes at current percentage; no auto-rollback fires",
        "userSees": "Rollout appears stuck; must manually promote or abort via kubectl argo rollouts abort",
        "userCan": "kubectl rollout restart deploy/argo-rollouts -n argo-rollouts; then manually promote or abort the stuck rollout"
      }
    ],
    "decision": {
      "chosen": "Decision: Argo Rollouts canary with Prometheus analysis",
      "rejected": "Rejected: Manual canary via ingress weight annotations — requires human watching metrics and manually adjusting weights; no automatic rollback",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-12-external-secrets-operator-eso",
    "num": "12",
    "name": "External Secrets Operator (ESO)",
    "label": "External Secrets Operator (ESO)",
    "layer": "Secrets",
    "status": "EXISTS",
    "what": "Operator that watches ExternalSecret custom resources and synchronizes secrets from AWS Secrets Manager, GCP Secret Manager, or Azure Key Vault into Kubernetes Secret objects.",
    "why": "Without ESO, secrets must be base64-encoded and stored in etcd (the Kubernetes database). etcd is not encrypted at rest by default — storing raw secrets there is a security violation.",
    "installCommand": "helm repo add external-secrets https://charts.external-secrets.io\nhelm upgrade --install external-secrets external-secrets/external-secrets \\\n--namespace external-secrets --create-namespace \\\n--version 0.9.18",
    "wiredTo": "Requires IRSA (EKS), Workload Identity (GKE), or Managed Identity (AKS) to authenticate to the cloud secret store. ArgoCD deploys ExternalSecret objects. Must be installed before ArgoCD deploys any service that needs a secret.",
    "verifyCommand": "kubectl get externalsecret -n prod",
    "verifyExpected": "Expected: all ExternalSecrets show STATUS=SecretSynced",
    "failures": [
      {
        "trigger": "Cloud IAM permission revoked from ESO's ServiceAccount",
        "system": "ESO cannot read from the secret store; existing Kubernetes Secrets retain their last-synced values; new secrets cannot be created",
        "userSees": "kubectl get externalsecret shows STATUS=SecretSyncedError; new pod deployments fail if they reference the missing secret",
        "userCan": "Restore IAM permission; kubectl annotate externalsecret <name> force-sync=$(date +%s) to trigger resync"
      }
    ],
    "decision": {
      "chosen": "Decision: ESO over Kubernetes native Secrets with Sealed Secrets",
      "rejected": "Rejected: Bitnami Sealed Secrets — encrypts secrets in Git, but still stores them in etcd; requires managing decryption keys per cluster",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-13-ghcr-pull-secret",
    "num": "13",
    "name": "GHCR Pull Secret",
    "label": "GHCR Pull Secret",
    "layer": "Secrets",
    "status": "EXISTS",
    "what": "A Kubernetes Secret of type docker-registry containing GitHub Container Registry credentials — required for the cluster to pull private container images.",
    "why": "Without this secret, every pod that tries to pull an image from ghcr.io/yarova-ca/* gets ImagePullBackOff — no service can start.",
    "installCommand": "kubectl create secret docker-registry ghcr-pull-secret \\\n--docker-server=ghcr.io \\\n--docker-username=<github-username> \\\n--docker-password=<github-pat-with-packages-read> \\\n--namespace prod\n# Repeat for dev and test namespaces\n# Or use ESO to sync from the cloud secret store automatically",
    "wiredTo": "Every pod spec references this secret via imagePullSecrets. The default ServiceAccount in each namespace must be patched to include the pull secret, or each Deployment must explicitly reference it.",
    "verifyCommand": "kubectl get secret ghcr-pull-secret -n prod -o jsonpath='{.type}'",
    "verifyExpected": "Expected: kubernetes.io/dockerconfigjson",
    "failures": [
      {
        "trigger": "GitHub PAT expires or is revoked",
        "system": "New pod starts trigger image pulls that fail with 401 Unauthorized; existing running pods are unaffected",
        "userSees": "kubectl get events -n prod shows ErrImagePull for any pod that restarts or is newly scheduled",
        "userCan": "Generate new PAT; kubectl delete secret ghcr-pull-secret -n prod; kubectl create secret docker-registry ghcr-pull-secret ... with new PAT"
      }
    ],
    "decision": {
      "chosen": "Decision: Single shared pull secret managed via ESO, synced from cloud secret store",
      "rejected": "Rejected: Per-service pull secrets — 105 secrets to rotate every time the PAT expires",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-14-postgresql-cloudnativepg-cnpg",
    "num": "14",
    "name": "PostgreSQL — CloudNativePG (CNPG)",
    "label": "PostgreSQL — CloudNativePG (CNPG)",
    "layer": "Data",
    "status": "EXISTS",
    "what": "HA PostgreSQL cluster with 1 primary + 2 replicas running inside Kubernetes. CloudNativePG handles streaming replication, automatic failover, and connection management.",
    "why": "Services need a database. Running PostgreSQL in Kubernetes with CNPG gives automated failover in under 30 seconds — critical for the 99.9% SLO.",
    "installCommand": "helm upgrade --install cnpg cloudnative-pg/cloudnative-pg \\\n--namespace cnpg-system --create-namespace \\\n--version 0.21.0\nkubectl apply -f infra/postgres/cluster.yaml",
    "wiredTo": "StorageClass (Component 2) must exist — CNPG creates PVCs for each instance. PgBouncer (Component 15) connects to the CNPG primary service. Backup (Component 25) configures WAL archival on the CNPG cluster object.",
    "verifyCommand": "kubectl get cluster -n postgres",
    "verifyExpected": "Expected: READY=3/3, STATUS=Cluster in healthy state",
    "failures": [
      {
        "trigger": "Primary PostgreSQL pod crashes",
        "system": "CNPG promotes one replica to primary within 30 seconds; new connections route to the new primary",
        "userSees": "~30s of database errors, then automatic recovery. Services log \"connection refused\" briefly then resume.",
        "userCan": "Monitor with kubectl get cluster -n postgres --watch; CNPG handles failover automatically"
      }
    ],
    "decision": {
      "chosen": "Decision: CNPG (CloudNativePG) over managed RDS/Cloud SQL",
      "rejected": "Rejected: AWS RDS — managed and reliable, but cloud-provider-locked and 3× more expensive per month at production sizing",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-15-pgbouncer-connection-pooler",
    "num": "15",
    "name": "PgBouncer — Connection Pooler",
    "label": "PgBouncer — Connection Pooler",
    "layer": "Data",
    "status": "EXISTS",
    "what": "Connection pooler that sits between all 106 services and PostgreSQL — multiplexes many service connections into a small pool of actual PostgreSQL connections.",
    "why": "PostgreSQL max_connections defaults to 100. With 106 services each maintaining a connection pool, the total exceeds max_connections within minutes of startup — without PgBouncer, the database refuses new connections.",
    "installCommand": "kubectl apply -f infra/pgbouncer/deployment.yaml\nkubectl apply -f infra/pgbouncer/service.yaml\nkubectl apply -f infra/pgbouncer/configmap.yaml",
    "wiredTo": "Services connect to PgBouncer's ClusterIP (not directly to CNPG). PgBouncer connects to the CNPG primary read-write service. Credentials stored as an ESO-managed Kubernetes Secret.",
    "verifyCommand": "kubectl exec -n postgres deploy/pgbouncer -- psql -h localhost -p 5432 -U pgbouncer pgbouncer -c \"SHOW POOLS;\"",
    "verifyExpected": "Expected: pools table showing active client and server connections",
    "failures": [
      {
        "trigger": "PgBouncer deployment is unavailable (pod crash or misconfiguration)",
        "system": "All services that connect through PgBouncer lose database access immediately; services that connect directly to CNPG are unaffected",
        "userSees": "HTTP 500 errors from all services that require database reads or writes",
        "userCan": "kubectl rollout restart deploy/pgbouncer -n postgres; verify with SHOW POOLS command above"
      }
    ],
    "decision": {
      "chosen": "Decision: PgBouncer in transaction pooling mode",
      "rejected": "Rejected: Session pooling mode — holds a server connection per client session; does not solve the max_connections problem under load",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-16-redis-sentinel-ha",
    "num": "16",
    "name": "Redis — Sentinel HA",
    "label": "Redis — Sentinel HA",
    "layer": "Data",
    "status": "EXISTS",
    "what": "HA Redis cluster (1 primary + 2 replicas + 3 Sentinel processes) for distributed caching, token blacklist, and rate limiting across all 106 services.",
    "why": "Without a shared Redis, token revocation only works on the pod that processed the logout — other pods still accept the revoked token. Rate limiting is per-pod, not per-user-globally.",
    "installCommand": "helm repo add bitnami https://charts.bitnami.com/bitnami\nhelm upgrade --install redis bitnami/redis \\\n--namespace redis --create-namespace \\\n--set architecture=replication \\\n--set sentinel.enabled=true \\\n--set auth.existingSecret=redis-password \\\n--version 18.19.4",
    "wiredTo": "All 106 services connect to the Redis Sentinel endpoint (not the primary directly). Token blacklist entries written by auth-service are read by all API services. ESO syncs the redis-password secret.",
    "verifyCommand": "kubectl exec -n redis redis-master-0 -- redis-cli -a <password> ping",
    "verifyExpected": "Expected: PONG",
    "failures": [
      {
        "trigger": "Redis primary pod crashes",
        "system": "Sentinel promotes a replica to primary within 10–30 seconds; services briefly see connection errors then reconnect",
        "userSees": "~15s of rate limit bypass possible during failover; token revocations during this window may not propagate",
        "userCan": "Monitor with kubectl get pods -n redis; Sentinel handles failover automatically"
      }
    ],
    "decision": {
      "chosen": "Decision: Redis Sentinel over Redis Cluster",
      "rejected": "Rejected: Redis Cluster — horizontal sharding; adds complexity for key routing that is unnecessary at current scale (well under 100GB keyspace)",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-17-prometheus-grafana",
    "num": "17",
    "name": "Prometheus + Grafana",
    "label": "Prometheus + Grafana",
    "layer": "Observability",
    "status": "EXISTS",
    "what": "Prometheus scrapes metrics from all pods every 15 seconds and stores them in a time-series database. Grafana visualizes the metrics as dashboards and SLO burn rate charts.",
    "why": "Without Prometheus, you cannot answer \"is the service slow right now, or did it slow down 20 minutes ago?\" You cannot detect an SLO breach before it becomes an outage.",
    "installCommand": "helm repo add prometheus-community https://prometheus-community.github.io/helm-charts\nhelm upgrade --install kube-prometheus-stack \\\nprometheus-community/kube-prometheus-stack \\\n--namespace monitoring --create-namespace \\\n--values infra/monitoring/prometheus-values.yaml \\\n--version 57.2.0",
    "wiredTo": "Argo Rollouts reads Prometheus metrics for canary analysis. Alertmanager (Component 18) routes alerts triggered by Prometheus rules. Grafana datasources include Loki (Component 19) and Tempo (Component 20) via Component 29.",
    "verifyCommand": "kubectl port-forward svc/kube-prometheus-stack-prometheus -n monitoring 9090:9090\n# Then open http://localhost:9090/targets",
    "verifyExpected": "Expected: all service targets show State=UP",
    "failures": [
      {
        "trigger": "Prometheus pod runs out of disk (default 50Gi PVC)",
        "system": "Prometheus stops ingesting metrics; existing data is readable until TSDB compaction fails",
        "userSees": "Grafana dashboards show \"No data\" for recent timeframes; Alertmanager stops firing alerts",
        "userCan": "kubectl get pvc -n monitoring; expand PVC or reduce retention from 15d to 7d in Helm values; restart Prometheus pod"
      }
    ],
    "decision": {
      "chosen": "Decision: kube-prometheus-stack (all-in-one Helm chart) over installing Prometheus and Grafana separately",
      "rejected": "Rejected: Separate Prometheus and Grafana installs — requires manually wiring datasources, recording rules, and default dashboards",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-18-alertmanager",
    "num": "18",
    "name": "Alertmanager",
    "label": "Alertmanager",
    "layer": "Observability",
    "status": "EXISTS",
    "what": "Receives alerts from Prometheus, deduplicates them, groups related alerts, and routes them to Slack channels or PagerDuty based on severity and namespace.",
    "why": "Prometheus fires alerts into a void without Alertmanager. A disk full event at 3am is never seen until a developer checks dashboards in the morning.",
    "installCommand": "kubectl apply -f infra/monitoring/alertmanager-config.yaml",
    "installNote": "Alertmanager is included in the kube-prometheus-stack Helm chart (Component 17). Configure routing in values.yaml:",
    "wiredTo": "Prometheus sends alerts to Alertmanager via its built-in alertmanager endpoint. Alertmanager reads a Secret containing Slack webhook URL and PagerDuty integration key (synced by ESO).",
    "verifyCommand": "kubectl port-forward svc/kube-prometheus-stack-alertmanager -n monitoring 9093:9093\n# Then open http://localhost:9093/#/alerts",
    "verifyExpected": "Expected: Alertmanager UI loads; check that Slack/PagerDuty receivers are listed under Status > Config",
    "failures": [
      {
        "trigger": "Slack webhook URL rotated; old URL stored in Alertmanager config",
        "system": "Alertmanager fires alerts but all Slack notifications fail with 403; PagerDuty still works if configured separately",
        "userSees": "No Slack alerts; only discoverable by checking Alertmanager logs",
        "userCan": "Update Slack webhook in cloud secret store; ESO syncs to Kubernetes Secret; kubectl rollout restart alertmanager pods"
      }
    ],
    "decision": {
      "chosen": "Decision: Alertmanager routing: critical=PagerDuty, warning=Slack",
      "rejected": "Rejected: All alerts to Slack — alert fatigue; critical alerts buried under warnings; no escalation path",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-19-loki-promtail",
    "num": "19",
    "name": "Loki + Promtail",
    "label": "Loki + Promtail",
    "layer": "Observability",
    "status": "EXISTS",
    "what": "Loki is a log aggregation system. Promtail is a DaemonSet agent that reads logs from all pod stdout and ships them to Loki with Kubernetes metadata labels.",
    "why": "Without Loki, finding what happened on a specific request across 14 microservices requires SSHing to nodes and grepping individual pod logs — impossible when pods have already restarted.",
    "installCommand": "helm repo add grafana https://grafana.github.io/helm-charts\nhelm upgrade --install loki grafana/loki-stack \\\n--namespace monitoring \\\n--set promtail.enabled=true \\\n--set loki.persistence.enabled=true \\\n--set loki.persistence.size=50Gi \\\n--version 2.10.2",
    "wiredTo": "Promtail runs as a DaemonSet and reads /var/log/pods/* on each node. Loki stores logs and is queried by Grafana (Component 29 wires this). Structured JSON logs from services include a trace_id field that links to Tempo (Component 20).",
    "verifyCommand": "kubectl logs -n monitoring -l app=promtail --tail=20",
    "verifyExpected": "Expected: \"level=info msg=Sending batch\" entries with non-zero byte counts",
    "failures": [
      {
        "trigger": "Loki PVC fills up (50Gi default, 7-day retention)",
        "system": "Loki stops accepting new logs; Promtail buffers briefly then drops logs",
        "userSees": "Grafana log queries return no recent results; Promtail logs show \"429 Too Many Requests\" from Loki",
        "userCan": "Expand Loki PVC or reduce retention; kubectl delete pod loki-0 -n monitoring to restart after expansion"
      }
    ],
    "decision": {
      "chosen": "Decision: Loki over Elasticsearch/OpenSearch for logs",
      "rejected": "Rejected: Elasticsearch — indexes all log fields, which is powerful but requires 8–16GB RAM per node at this log volume",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-20-tempo-distributed-tracing",
    "num": "20",
    "name": "Tempo — Distributed Tracing",
    "label": "Tempo — Distributed Tracing",
    "layer": "Observability",
    "status": "EXISTS",
    "what": "Distributed tracing backend that stores OpenTelemetry spans. Every request gets a trace ID; Tempo shows the full call chain across all 14 microservices it touched.",
    "why": "A 500ms latency spike in prod spans 8 services. Without tracing you cannot tell which service added 400ms. With Tempo you see the exact span — 400ms was the PostgreSQL query in user-service.",
    "installCommand": "helm upgrade --install tempo grafana/tempo \\\n--namespace monitoring \\\n--set tempo.storage.trace.backend=local \\\n--set tempo.storage.trace.local.path=/var/tempo/traces \\\n--version 1.7.1",
    "wiredTo": "Services send OTel spans to the Tempo endpoint (OTLP gRPC port 4317). Loki logs include trace_id; Grafana can jump from a log line to the matching trace. Component 29 wires Tempo as a Grafana datasource.",
    "verifyCommand": "kubectl port-forward svc/tempo -n monitoring 3200:3200\ncurl http://localhost:3200/api/echo",
    "verifyExpected": "Expected: {\"status\":\"ok\"}",
    "failures": [
      {
        "trigger": "Tempo pod crashes or disk fills up",
        "system": "New trace spans are dropped; existing stored traces remain readable until TTL",
        "userSees": "Grafana Explore shows \"No data\" for traces; services continue working normally",
        "userCan": "kubectl rollout restart deploy/tempo -n monitoring; clear old traces if disk is full"
      }
    ],
    "decision": {
      "chosen": "Decision: Tempo over Jaeger",
      "rejected": "Rejected: Jaeger — does not integrate natively with Grafana; separate UI required; no out-of-the-box Loki log correlation",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-21-trivy-operator",
    "num": "21",
    "name": "Trivy Operator",
    "label": "Trivy Operator",
    "layer": "Observability",
    "status": "EXISTS",
    "what": "Continuously scans every container image running in the cluster for CVEs and exposes scan results as Kubernetes CRDs (VulnerabilityReport) queryable via kubectl.",
    "why": "CI scans images at build time. Trivy Operator scans images while they are running — a CVE published after the last build is caught within hours, not in the next quarterly security review.",
    "installCommand": "helm repo add aqua https://aquasecurity.github.io/helm-charts\nhelm upgrade --install trivy-operator aqua/trivy-operator \\\n--namespace trivy-system --create-namespace \\\n--version 0.21.4",
    "wiredTo": "Scan results are exposed as Prometheus metrics (trivy_image_vulnerabilities gauge) scraped by Prometheus. Grafana dashboard shows CVE counts per image. Kyverno can reference scan results to block images with critical CVEs.",
    "verifyCommand": "kubectl get vulnerabilityreport -n prod --no-headers | wc -l",
    "verifyExpected": "Expected: count equal to number of unique container images running in prod namespace",
    "failures": [
      {
        "trigger": "Trivy Operator cannot pull the vulnerability database (air-gapped cluster or network policy blocks egress)",
        "system": "Scan jobs fail; VulnerabilityReports become stale; new CVEs not detected",
        "userSees": "kubectl get vulnerabilityreport shows old scan dates; kubectl describe vulnerabilityreport shows \"failed to download DB\"",
        "userCan": "Mirror Trivy DB to internal registry; configure operator to use mirror via Helm values trivyOperator.trivyImageRepository"
      }
    ],
    "decision": {
      "chosen": "Decision: Trivy Operator (cluster-side continuous scan) in addition to CI-time Trivy scan",
      "rejected": "Rejected: CI-only scanning — CVEs published after build are invisible until next build cycle, which can be weeks for stable services",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-22-velero-cluster-backup",
    "num": "22",
    "name": "Velero — Cluster Backup",
    "label": "Velero — Cluster Backup",
    "layer": "Reliability",
    "status": "EXISTS",
    "what": "Backs up all Kubernetes resource definitions (Deployments, ConfigMaps, Secrets, CRDs, RBAC) and PVC snapshots to S3/GCS. Enables full cluster restore from scratch.",
    "why": "Without Velero, a catastrophic cluster failure (accidental deletion, cloud provider incident) requires manually recreating every resource. With Velero, restore is one command and takes under 30 minutes.",
    "installCommand": "helm repo add vmware-tanzu https://vmware-tanzu.github.io/helm-charts\nhelm upgrade --install velero vmware-tanzu/velero \\\n--namespace velero --create-namespace \\\n--values infra/velero/values.yaml \\\n--version 6.4.0\n# values.yaml sets S3 bucket, schedule: \"0 2 * * *\", TTL: 720h (30 days)",
    "wiredTo": "Requires cloud IAM permissions to write to S3/GCS (via IRSA/Workload Identity). PVC snapshots use the cloud provider's snapshot API. ESO-synced secrets are backed up as Kubernetes Secret objects (already pulled from the cloud store).",
    "verifyCommand": "velero backup get",
    "verifyExpected": "Expected: most recent backup with STATUS=Completed and ERRORS=0",
    "failures": [
      {
        "trigger": "Velero backup job fails (S3 permission error, disk snapshot quota hit)",
        "system": "The cluster continues running; only the backup copy is missing; previous backup is still valid",
        "userSees": "velero backup get shows STATUS=Failed; Alertmanager fires VeleroBackupFailed alert",
        "userCan": "velero backup describe <name> --details to see error; fix IAM permission or quota; velero backup create manual-backup to retry"
      }
    ],
    "decision": {
      "chosen": "Decision: Velero daily backup at 02:00 UTC, 30-day retention",
      "rejected": "Rejected: Hourly backups — EBS/PD snapshot costs scale linearly; 30 snapshots per PVC per day at prod scale exceeds $500/month",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-23-externaldns",
    "num": "23",
    "name": "ExternalDNS",
    "label": "ExternalDNS",
    "layer": "Reliability",
    "status": "EXISTS",
    "what": "Watches Ingress objects and automatically creates/updates DNS A records in Route53/Cloud DNS/Azure DNS pointing to the ingress-nginx LoadBalancer IP.",
    "why": "Without ExternalDNS, deploying a new service requires a manual DNS entry. With 106 services × 3 environments, that is 315 DNS entries to keep synchronized manually.",
    "installCommand": "helm repo add external-dns https://kubernetes-sigs.github.io/external-dns/\nhelm upgrade --install external-dns external-dns/external-dns \\\n--namespace external-dns --create-namespace \\\n--set provider=aws \\\n--set policy=upsert-only \\\n--set txtOwnerId=prod-cluster \\\n--version 1.14.4",
    "wiredTo": "Reads hostname from Ingress spec.rules[].host. Creates DNS records pointing to the ingress-nginx LoadBalancer external IP. Requires Route53 write permissions via IRSA (EKS) or equivalent.",
    "verifyCommand": "kubectl logs -n external-dns deploy/external-dns | grep \"Desired change\"",
    "verifyExpected": "Expected: \"Desired change: 0\" when DNS is in sync with Ingress objects",
    "failures": [
      {
        "trigger": "ExternalDNS IAM permission revoked",
        "system": "Existing DNS records remain valid; new Ingress objects do not get DNS entries; deleted services keep their DNS records",
        "userSees": "New service URL returns NXDOMAIN; ExternalDNS logs show \"AccessDenied\" from Route53",
        "userCan": "Restore IAM permission; ExternalDNS will reconcile on next sync cycle (default 1 minute)"
      }
    ],
    "decision": {
      "chosen": "Decision: policy=upsert-only (never deletes DNS records)",
      "rejected": "Rejected: policy=sync — automatically deletes DNS records for removed Ingresses; one accidental Ingress deletion = prod URL goes dark",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-24-cluster-autoscaler",
    "num": "24",
    "name": "Cluster Autoscaler",
    "label": "Cluster Autoscaler",
    "layer": "Reliability",
    "status": "EXISTS",
    "what": "Monitors pods in Pending state due to insufficient node resources and automatically adds nodes to the cluster; removes underutilized nodes during low traffic periods.",
    "why": "Without the autoscaler, node capacity must be over-provisioned to handle peak load — wasting money at off-peak hours. Or nodes run full and new pods stay Pending indefinitely during traffic spikes.",
    "installCommand": "helm repo add autoscaler https://kubernetes.github.io/autoscaler\nhelm upgrade --install cluster-autoscaler autoscaler/cluster-autoscaler \\\n--namespace kube-system \\\n--set autoDiscovery.clusterName=prod \\\n--set awsRegion=us-east-1 \\\n--version 9.36.0",
    "wiredTo": "Reads ResourceQuota and node taints to calculate whether a pending pod can fit on a new node. Node groups must have autoscaling enabled in the cloud provider (ASG on EKS, MIG on GKE). ResourceQuota caps still apply — autoscaler cannot add capacity beyond namespace quota.",
    "verifyCommand": "kubectl logs -n kube-system deploy/cluster-autoscaler | grep \"Scale-up\"",
    "verifyExpected": "Expected: scale-up events matching traffic spikes; or \"No scale up needed\" during normal load",
    "failures": [
      {
        "trigger": "Node group max size reached; cluster cannot add more nodes",
        "system": "Pods remain in Pending state; existing services are unaffected; autoscaler logs \"max node group size reached\"",
        "userSees": "New deployments or scaled replicas stay Pending; kubectl get pods shows Pending with \"0/N nodes are available\"",
        "userCan": "Increase node group max size in cloud console or Terraform; autoscaler detects the higher limit within 10 minutes"
      }
    ],
    "decision": {
      "chosen": "Decision: Cluster Autoscaler over Karpenter (EKS)",
      "rejected": "Rejected: Karpenter — more flexible instance selection and faster provisioning, but EKS-only; incompatible with the multi-cloud cluster design",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-25-postgresql-backup-cnpg-wal-archival",
    "num": "25",
    "name": "PostgreSQL Backup (CNPG WAL Archival)",
    "label": "PostgreSQL Backup (CNPG WAL Archival)",
    "layer": "Reliability",
    "status": "EXISTS",
    "what": "Configures CloudNativePG to continuously archive WAL (Write-Ahead Log) segments to S3/GCS and take daily base backups — enabling point-in-time recovery (PITR) to any second in the last 7 days.",
    "why": "Velero backs up Kubernetes resources. PostgreSQL data lives inside PVCs and is not safely backed up by volume snapshots alone — a snapshot mid-transaction produces a corrupted backup. WAL archival is the only correct PostgreSQL backup strategy.",
    "installCommand": "kubectl apply -f infra/postgres/cluster-with-backup.yaml\n# cluster-with-backup.yaml sets:\n#   backup.barmanObjectStore.destinationPath: s3://yarova-pg-backup\n#   backup.barmanObjectStore.wal.compression: gzip\n#   scheduledBackup: \"0 3 * * *\" (daily at 03:00 UTC)",
    "installNote": "CNPG backup is configured in the Cluster CRD, not a separate Helm chart:",
    "wiredTo": "Requires S3 write access via IRSA. The CNPG Cluster object must be updated (not a separate install). Velero backs up the Cluster CRD object; the actual data is in S3 via CNPG.",
    "verifyCommand": "kubectl get scheduledbackup -n postgres\nkubectl get backup -n postgres --sort-by=.metadata.creationTimestamp | tail -3",
    "verifyExpected": "Expected: most recent backup shows phase=completed",
    "failures": [
      {
        "trigger": "S3 bucket deleted or CNPG IAM role loses write permission",
        "system": "WAL archival fails silently; database continues serving queries; CNPG logs show archival errors",
        "userSees": "kubectl describe cluster -n postgres shows \"WAL archiving failing\" in status conditions",
        "userCan": "Fix S3 permission; CNPG will resume archival and catch up on buffered WAL segments automatically"
      }
    ],
    "decision": {
      "chosen": "Decision: CNPG WAL archival to S3, 7-day PITR window",
      "rejected": "Rejected: PVC snapshots as the sole database backup — snapshots are not crash-consistent for PostgreSQL; risk of data corruption on restore",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-26-opencost",
    "num": "26",
    "name": "OpenCost",
    "label": "OpenCost",
    "layer": "Cost",
    "status": "EXISTS",
    "what": "Real-time cost allocation tool that breaks down the cloud bill by namespace, service, label, and team — visible in Grafana and queryable via API.",
    "why": "Without OpenCost, the AWS/GCP bill is one number per month. You cannot tell whether the $4,000 spike was caused by the new ML service in prod or a runaway dev namespace.",
    "installCommand": "helm repo add opencost https://opencost.github.io/opencost-helm-chart\nhelm upgrade --install opencost opencost/opencost \\\n--namespace opencost --create-namespace \\\n--set opencost.exporter.cloudProviderApiKey=\"\" \\\n--version 1.38.1",
    "wiredTo": "Reads Prometheus metrics for CPU/memory usage (requires Prometheus to be running). Uses cloud pricing APIs to calculate cost per resource unit. Grafana dashboard shows cost breakdown by namespace and label.",
    "verifyCommand": "kubectl port-forward svc/opencost -n opencost 9090:9090\ncurl http://localhost:9090/allocation?window=1d | jq '.data[0] | keys'",
    "verifyExpected": "Expected: JSON with namespace names and cost breakdowns",
    "failures": [
      {
        "trigger": "OpenCost cannot reach the cloud pricing API (network policy blocks egress)",
        "system": "OpenCost falls back to default on-demand pricing estimates; actual cost data is unavailable",
        "userSees": "Grafana cost dashboard shows estimates marked \"(estimate)\" rather than actuals",
        "userCan": "Allow opencost namespace egress to pricing endpoints in network policy; restart OpenCost pod"
      }
    ],
    "decision": {
      "chosen": "Decision: OpenCost over Kubecost",
      "rejected": "Rejected: Kubecost — OpenCost is the open-source core of Kubecost; enterprise Kubecost adds a $2,000+/year license for features (governance, anomaly detection) not needed at this scale",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-27-terraform-cluster-provisioning",
    "num": "27",
    "name": "Terraform — Cluster Provisioning",
    "label": "Terraform — Cluster Provisioning",
    "layer": "Infra",
    "status": "EXISTS",
    "what": "Infrastructure-as-code tool that creates and manages the Kubernetes cluster itself (EKS/GKE/AKS), node groups, VPC networking, IAM roles, and cloud resources from .tf files in Git.",
    "why": "Without Terraform, cluster creation is a series of console clicks that cannot be reproduced exactly. A new environment takes days. With Terraform, a new cluster is terraform apply — identical to the existing ones.",
    "installCommand": "cd infra/clusters/terraform/prod\ncp terraform.tfvars.example terraform.tfvars\n# Edit: set region, account_id, node_count, node_type\nterraform init\nterraform apply",
    "wiredTo": "Terraform creates the cluster; all other components (cert-manager, ArgoCD, etc.) are installed into the cluster via the Helm install commands documented in each component above (run in the dependency order shown in the Full Install Sequence section below) after terraform apply completes. Terraform state stored in Component 28.",
    "verifyCommand": "terraform show | grep cluster_name\nkubectl get nodes",
    "verifyExpected": "Expected: nodes in Ready state matching the node_count in terraform.tfvars",
    "failures": [
      {
        "trigger": "terraform apply fails mid-run due to cloud API throttling or quota exceeded",
        "system": "Terraform partially creates resources; state file records what was created; next apply completes remaining resources",
        "userSees": "terraform apply exits with error; running terraform apply again resolves partial state",
        "userCan": "Run terraform apply again — Terraform is idempotent; re-running completes the partial apply safely"
      }
    ],
    "decision": {
      "chosen": "Decision: Terraform over Pulumi or cloud-native CLI tools (eksctl, gcloud container clusters create)",
      "rejected": "Rejected: eksctl — AWS-only; scripts would need rewriting for GKE and AKS",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-28-terraform-state-backend-s3-gcs",
    "num": "28",
    "name": "Terraform State Backend (S3 / GCS)",
    "label": "Terraform State Backend (S3 / GCS)",
    "layer": "Infra",
    "status": "EXISTS",
    "what": "Remote storage for the Terraform state file — an S3 bucket (EKS) or GCS bucket (GKE) with DynamoDB/GCS locking to prevent concurrent terraform applies from corrupting state.",
    "why": "Without a remote state backend, two developers running terraform apply simultaneously create duplicate resources (two clusters, two VPCs) — neither knows about the other's changes.",
    "installCommand": "# AWS\naws s3 mb s3://yarova-terraform-state --region us-east-1\naws dynamodb create-table --table-name terraform-lock \\\n--attribute-definitions AttributeName=LockID,AttributeType=S \\\n--key-schema AttributeName=LockID,KeyType=HASH \\\n--billing-mode PAY_PER_REQUEST\n\n# GCP\ngcloud storage buckets create gs://yarova-terraform-state \\\n--uniform-bucket-level-access",
    "installNote": "Must be created before any Terraform runs. Created manually once per team:",
    "wiredTo": "Every Terraform module's backend.tf references this bucket. DynamoDB (AWS) or GCS object locking provides mutual exclusion for concurrent applies.",
    "verifyCommand": "terraform state list | head -5",
    "verifyExpected": "Expected: list of managed resources (module.eks.aws_eks_cluster.main, etc.)",
    "failures": [
      {
        "trigger": "State file locked (previous terraform apply crashed mid-run)",
        "system": "All subsequent terraform commands fail with \"Error acquiring the state lock\"",
        "userSees": "terraform apply output: \"Error locking state: Error acquiring the state lock\"",
        "userCan": "Verify the previous run is truly stopped; then terraform force-unlock <lock-id> (use with caution)"
      }
    ],
    "decision": {
      "chosen": "Decision: S3 + DynamoDB state backend (AWS) / GCS with object locking (GCP)",
      "rejected": "Rejected: Terraform Cloud remote state — adds external SaaS dependency; free tier is limited; team prefers self-hosted state",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-29-grafana-datasources-loki-tempo",
    "num": "29",
    "name": "Grafana Datasources — Loki + Tempo",
    "label": "Grafana Datasources — Loki + Tempo",
    "layer": "Observability",
    "status": "EXISTS",
    "what": "Configures Grafana to use Loki as a log datasource and Tempo as a tracing datasource — enabling log search and trace browsing from the Grafana UI without separate dashboards.",
    "why": "Loki and Tempo run and collect data regardless. Without the datasource wiring, they are invisible in Grafana. This is the final 5-minute step that unlocks the full observability stack.",
    "installCommand": "kubectl apply -f infra/monitoring/grafana-datasources.yaml",
    "installNote": "grafana-datasources.yaml contains two GrafanaDatasource CRDs:",
    "wiredTo": "Loki (Component 19) and Tempo (Component 20) must be running before datasources are wired. Grafana Operator CRDs required — included in kube-prometheus-stack.",
    "verifyCommand": "kubectl port-forward svc/kube-prometheus-stack-grafana -n monitoring 3000:80\n# Open http://localhost:3000 > Connections > Data Sources",
    "verifyExpected": "Expected: Loki and Tempo listed with green \"Data source connected and labels found\" status",
    "failures": [
      {
        "trigger": "Loki or Tempo service ClusterIP changes after a reinstall",
        "system": "Grafana datasource URL points to old IP; queries return \"connection refused\"",
        "userSees": "Grafana shows \"Data source connected but no labels found\" or timeout errors in Explore",
        "userCan": "kubectl get svc loki -n monitoring to confirm current URL; update datasource URL; kubectl apply -f infra/monitoring/grafana-datasources.yaml"
      }
    ],
    "decision": {
      "chosen": "Decision: GrafanaDatasource CRDs managed in Git, not Grafana UI",
      "rejected": "Rejected: Configure datasources via Grafana UI — configuration is lost on Grafana pod restart unless persistence is configured",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  },
  {
    "id": "comp-30-backstage-developer-portal",
    "num": "30",
    "name": "Backstage Developer Portal",
    "label": "Backstage Developer Portal",
    "layer": "Developer Platform",
    "status": "EXISTS",
    "what": "Internal developer portal providing a service catalog of all 106 services, TechDocs for browsable documentation, and software templates for scaffolding new services from the CLI.",
    "why": "Without Backstage, developers discover services by searching GitHub or asking teammates. With 106 services across 3 environments, service ownership, runbooks, and onboarding paths are invisible without a catalog.",
    "installCommand": "See backstage/GITHUB-OAUTH-SETUP.md and scripts/setup-backstage.sh",
    "installNote": "Required before running setup: PostgreSQL running, GITHUB_TOKEN set, AUTH_GITHUB_CLIENT_ID and AUTH_GITHUB_CLIENT_SECRET set.",
    "wiredTo": "PostgreSQL (Component 14) — Backstage stores catalog state in a PostgreSQL database. GitHub OAuth — all authentication flows through GitHub. catalog-info.yaml files in each of the 106 service repos register the service in the catalog. backstage/catalog/all.yaml is the root catalog file that imports all service registrations.",
    "verifyCommand": "curl http://localhost:7007/api/catalog/entities",
    "verifyExpected": "Expected: JSON array of catalog entries — one object per registered service",
    "healthCommand": "curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3000",
    "healthExpected": "Expected: 200 — Backstage login page is served",
    "failures": [
      {
        "trigger": "Backstage shows \"No catalog results\" after login",
        "system": "Backstage frontend loads but the catalog API returns an empty array; backend is running but catalog ingestion failed",
        "userSees": "Empty catalog — no services, no components, no APIs listed in the UI",
        "userCan": "Verify backstage/catalog/all.yaml is readable and all catalog-info.yaml files have valid YAML; restart the Backstage backend pod to trigger a fresh catalog sync"
      },
      {
        "trigger": "Backstage backend fails to start — PostgreSQL unreachable or required env var missing",
        "system": "Backend pod crashes on startup with a database connection error or missing config error; frontend cannot reach the API",
        "userSees": "curl http://localhost:7007/api/catalog/entities returns connection refused or HTTP 500",
        "userCan": "Verify PostgreSQL is running (Component 14); verify GITHUB_TOKEN, AUTH_GITHUB_CLIENT_ID, AUTH_GITHUB_CLIENT_SECRET are all set; check backend pod logs for the exact missing variable name"
      }
    ],
    "decision": {
      "chosen": "Decision: Backstage with PostgreSQL backend over SQLite",
      "rejected": "Rejected: SQLite backend — Backstage default for local dev only; data is lost on pod restart; does not support multiple Backstage replicas",
      "tradeoff": null
    },
    "sourcePage": "19-cluster-setup.html"
  }
]
