[
  {
    "id": "auth",
    "name": "AUTH",
    "label": "Authentication method",
    "arg": "AUTH",
    "validValues": [
      "none",
      "jwt",
      "oauth2",
      "apikey",
      "all"
    ],
    "appliesTo": "All API services",
    "default": "all",
    "valueNotes": {
      "none": "No authentication — all requests pass through",
      "jwt": "JWT Bearer token validation middleware on all routes. Invalid or missing token → 401.",
      "oauth2": "OAuth2 client credentials flow — machine-to-machine API access",
      "apikey": "API key header validation — checks X-API-Key header on every request"
    },
    "implStatus": "Implemented",
    "scope": "Scope: applies to 20 of the 34 Tier 1 services — all server services with a request entry point"
  },
  {
    "id": "build-image",
    "name": "BUILD_IMAGE",
    "label": "Language / runtime family",
    "arg": "BUILD_IMAGE",
    "validValues": [
      "node",
      "python",
      "golang",
      "java",
      "dotnet",
      "rust",
      "elixir",
      "ruby",
      "php"
    ],
    "appliesTo": "All services",
    "default": "node",
    "valueNotes": {
      "node": "Default. Used for all TypeScript/Express/Fastify backends.",
      "python": "Used for FastAPI, Django, Flask services.",
      "golang": "Builder stage compiles binary. Runtime is distroless — no shell, minimal attack surface.",
      "java": "Builds JAR in builder, runs JRE-only in runtime.",
      "dotnet": "Publishes self-contained binary. Runtime is ASP.NET Core image.",
      "rust": "Compiles release binary. Runtime is bare Alpine — binary is statically linked.",
      "elixir": "Builds Mix release. Runtime runs the release tarball on bare Alpine.",
      "ruby": "Used for Rails API services. Puma runs in runtime stage.",
      "php": "PHP-FPM. Compose installs vendor in builder. Runtime runs FPM."
    },
    "implStatus": "Implemented"
  },
  {
    "id": "build-tool",
    "name": "BUILD_TOOL",
    "label": "TypeScript compiler / bundler",
    "arg": "BUILD_TOOL",
    "validValues": [
      "tsc",
      "esbuild",
      "swc"
    ],
    "appliesTo": "Node backends only",
    "default": "tsc",
    "valueNotes": {
      "tsc": "Default. Best type checking. Required for libraries that export types.",
      "esbuild": "When build speed matters. Lambda deployments. Does not type-check.",
      "swc": "When tsc is too slow but per-file output is needed. Does not type-check."
    },
    "implStatus": "Implemented",
    "scope": "Scope: applies to 13 of the 34 Tier 1 services"
  },
  {
    "id": "compliance",
    "name": "COMPLIANCE",
    "label": "Compliance preset",
    "arg": "COMPLIANCE",
    "validValues": [
      "standard",
      "hipaa",
      "pci",
      "pipeda",
      "fips",
      "soc2",
      "cmmc",
      "nerc"
    ],
    "appliesTo": "All API services",
    "default": "standard",
    "valueNotes": {
      "standard": "Nothing extra — base service only",
      "hipaa": "PHI field masking in all log output, audit trail middleware, verbose error messages disabled",
      "pci": "TLS enforced on all routes, PCI audit headers added, debug endpoints removed, plaintext-secrets startup check",
      "pipeda": "Consent check middleware, data residency header (X-Data-Residency: CA), PII masking in logs",
      "fips": "FIPS-140-2 OpenSSL module active, non-FIPS cipher suites disabled",
      "soc2": "Immutable audit log, access control middleware, debug mode disabled",
      "cmmc": "Full audit log on every request, debug routes removed, no outbound internet at runtime, CUI data handling",
      "nerc": "Network isolation config, no internet egress, OT/IT boundary enforcement headers"
    },
    "implStatus": "Implemented",
    "scope": "Scope: applies to 20 of the 34 Tier 1 services — all server services that process HTTP requests"
  },
  {
    "id": "observability",
    "name": "OBSERVABILITY",
    "label": "Observability stack",
    "arg": "OBSERVABILITY",
    "validValues": [
      "none",
      "otel",
      "prometheus",
      "datadog"
    ],
    "appliesTo": "All API services",
    "default": "none",
    "valueNotes": {
      "none": "Nothing — no instrumentation code added",
      "otel": "OpenTelemetry SDK + OTLP exporter for traces, metrics, and logs",
      "prometheus": "Prometheus metrics endpoint at /metrics",
      "datadog": "Datadog tracer + APM agent configured at service startup"
    },
    "implStatus": "Implemented",
    "scope": "Scope: applies to 20 of the 34 Tier 1 services — all server services with HTTP request handlers"
  },
  {
    "id": "orm",
    "name": "ORM",
    "label": "Database access layer",
    "arg": "ORM",
    "validValues": [
      "prisma",
      "typeorm",
      "drizzle",
      "none",
      "sqlalchemy",
      "tortoise",
      "gorm",
      "ent"
    ],
    "appliesTo": "Backend services",
    "default": "prisma (Node) / sqlalchemy (Python) / gorm (Go)",
    "implStatus": "Implemented",
    "scope": "Scope: applies to 14 of the 34 Tier 1 services — pure API server services only (groups 14–23)"
  },
  {
    "id": "pkg-mgr",
    "name": "PKG_MGR",
    "label": "Package manager",
    "arg": "PKG_MGR",
    "validValues": [
      "npm",
      "pnpm",
      "yarn",
      "bun",
      "pip",
      "poetry",
      "uv"
    ],
    "appliesTo": "Node, Python",
    "default": "npm (Node) / pip (Python)",
    "valueNotes": {
      "npm": "Default. ci enforces lockfile — no version drift in builds.",
      "pnpm": "Faster installs, hard-linked node_modules. Requires corepack enable in Dockerfile.",
      "yarn": "Yarn Classic (v1). Yarn Berry (v2+) not supported — PnP mode breaks the build.",
      "bun": "Fastest installs. Use only if production runtime also uses Bun — bun:latest image required.",
      "pip": "Default for Python. Simple and universal.",
      "poetry": "Strict dependency resolution. Requires pyproject.toml present.",
      "uv": "Rust-based pip replacement. 10-100x faster installs. Uses same requirements.txt format."
    },
    "implStatus": "Implemented",
    "scope": "Scope: applies to 14 of the 34 Tier 1 services"
  },
  {
    "id": "runtime",
    "name": "RUNTIME",
    "label": "OS variant / compliance mode",
    "arg": "RUNTIME",
    "validValues": [
      "alpine",
      "slim",
      "fips"
    ],
    "appliesTo": "All services",
    "default": "alpine",
    "valueNotes": {
      "alpine": "Default for all services. Smallest attack surface with shell access.",
      "slim": "When a package requires glibc (native modules, some ML libs). Alpine uses musl.",
      "fips": "Required when COMPLIANCE=fips . US federal workloads. NIST 140-2 cryptography."
    },
    "implStatus": "Implemented"
  }
]