# Enable OpenShift's built-in Prometheus to scrape user workloads.
# OpenShift ships a managed Prometheus; this avoids running a second stack.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-monitoring-config
  namespace: openshift-monitoring
data:
  config.yaml: |
    enableUserWorkload: true
